//! Integration with whatever Linux desktop the app runs on: which environment it is, and
//! registering the app as the default file manager.

pub mod default_app;
pub mod entry;
pub mod environment;
